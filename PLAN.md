# OC-GO Dashboard 完善规划 v0.2（架构升级版）

> 方向（按老大 2026-08-05 要求）：先搭可扩展基础框架 → 老大提需求逐个完善。
> 核心转变：从"单供应商小悬浮窗" → "托盘主面板 + 多供应商 + 设置 DIY"。

---

## 一、总体架构

```
oc-go-dashboard/
├── main.py              入口：托盘 + 窗口管理（主面板 / 迷你窗）
├── config.json          全部设置（热保存，界面改即时生效）
├── app/
│   ├── kernel.py        调度内核：多供应商抓取、缓存、通知判断
│   ├── providers/
│   │   ├── base.py      Provider 抽象基类 + 标准化数据模型
│   │   └── opencode_go.py   OpenCode Go（现有抓取逻辑迁移）
│   ├── notifier.py      通知层（方式可配：托盘气泡/系统通知/关闭）
│   ├── themes.py        主题定义 + 自定义主色
│   ├── settings.py      设置读写 + 默认值合并
│   └── store.py         快照存储（per-provider，30 天）
├── ui/
│   ├── main_panel.html  主面板：仪表盘 / 供应商 / 外观 / 通知 / 关于
│   └── mini_widget.html 迷你悬浮窗（可选保留，外观页开关）
└── hana-plugin/         跟随 Hana 起落
```

## 二、窗口体系（交互核心）

| 窗口 | 触发 | 行为 |
|---|---|---|
| **主面板**（新，约 920×600） | 双击托盘图标 / 托盘菜单"打开主面板" | 仪表盘 + 设置中心，关闭按钮=隐藏到托盘（不退出） |
| 迷你窗（保留，可选） | 外观页开关 | 右下角霓虹 HUD，可独立开关/调尺寸 |
| 托盘 | 右键菜单 | 打开主面板 / 显示迷你窗 / 立即刷新 / 退出 |

## 三、多供应商（Provider 插件化）

**抽象接口**（providers/base.py）：
```python
class Provider:
    id: str                    # "opencode-go"
    name: str                  # 显示名
    config_schema: list        # 需用户填的字段（workspace_id / cookie ...）
    def fetch(config) -> dict  # 返回标准化数据
```

**标准化数据模型**：
```json
{
  "provider": "opencode-go", "ok": true, "fetched_at": ...,
  "plan_name": "Go",
  "limits": [ {"id":"rolling","label":"5h 滚动","used_pct":6,"reset_in_sec":...}, ... ],
  "balance": {"currency":"USD","amount":0.0},
  "meta": {...}   // 供应商特有附加信息
}
```

**内核调度**：遍历启用的 providers → 并行抓取 → 各自缓存 + 快照 + 通知判断。
**UI 呈现**：仪表盘按供应商分卡片；供应商页可启用/停用/编辑凭据/测试连接。

后续加新供应商 = 新写一个 provider 文件 + 界面填凭据，框架零改动。

## 四、设置中心（DIY 清单）

### 外观
- **主题**：预置 6 套霓虹配色（绿/青/紫/琥珀/绯红/冰蓝）+ **自定义主色**（色板选色即时预览）
- **透明度**：滑条 40%–100%，主面板独立于迷你窗
- 迷你窗：开关 / 尺寸档位 / 位置（四角）
- 界面密度：紧凑 / 舒适

### 供应商
- 启用/停用、编辑凭据、测试连接、自定义备注名

### 通知
- 方式：托盘气泡（零依赖，默认）/ 系统通知（winotify）/ 关闭
- 事件独立开关：80% 预警、95% 紧急、cookie 失效、连续失败
- 阈值可调（默认 80/95）

### 数据
- 刷新间隔：1–60 分钟滑条（默认 5）
- 金额显示：美元 / 百分比 切换

### 其他
- 开机自启（独立于 Hana 绑定，可选开关）

> 全部设置界面改完即时生效并写入 config.json，不用重启。

## 五、开发顺序与工作量（约 4–5 小时）

1. **架构重构**（1h）：providers/base.py + opencode_go.py 迁移 + kernel 多供应商调度 + store per-provider
2. **主面板 UI**（1.5h）：仪表盘卡片网格 + 五个设置页 + JS 交互（热保存）
3. **托盘与窗口**（30min）：双击弹主面板、关闭=隐藏、迷你窗开关联动
4. **主题系统**（30min）：themes.py + CSS 变量注入 + 自定义主色
5. **通知配置化**（30min）：notifier 接设置 + 测试通知按钮
6. **联调自测**（30min）：多供应商空态/单供应商/断网/失效 cookie 场景

每步完成即验证（`--once` + 界面预览），不攒联调。

## 六、未来可选（v0.3+）

- 新供应商接入（opencode zen、Anthropic 订阅、OpenRouter...）
- exe 打包、多显示器定位、数据导出 CSV、用量周报
